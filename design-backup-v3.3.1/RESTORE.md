Exact v3.3.1 header reference: index.html contains the original A76 .brand-icon markup; styles.css contains its complete cascade. favicon.svg is the unchanged geometric favicon. To restore the logo only, replace the new .brand-mark image with the original .brand-icon markup and retain the old .brand-icon CSS rules, which still exist in the stylesheet. Do not replace the whole current application HTML.
