# v3.3.6 — GitHub photo publication fix

GitHub redirects the issue #2 image to github-production-user-asset-6210df.s3.amazonaws.com. The previous rule accepted only numeric bucket suffixes; the real bucket includes letters. Added this specific observed host while retaining HTTPS, bounded redirects, size limits and image re-encoding. Regression tests reject lookalike hosts, arbitrary S3 buckets, credentials, HTTP and nonstandard ports. Error messages now identify a blocked hostname without exposing signed URL query parameters.

Install the complete release, commit and push. Start a NEW Review and publish photos workflow on main, with issue 2, Publish checked and the latest preview hash. Do not use Re-run jobs on the old failure, which can use the old commit. Photos already attached to the issue do not need to be uploaded again.
