# Photo input requirement

User requirement: photo selection/upload interfaces must always offer drag-and-drop in addition to accessible browse controls. Preserve keyboard and mobile browse alternatives, file validation, status feedback and local-only behavior unless explicitly changed. Folder drops need an explicit fallback when unsupported.
